альтернативная версия с использованием контекстного менеджера:

python
import requests

def get_posts_alternative():
    url = "https://jsonplaceholder.typicode.com/posts"
    
    try:
        with requests.get(url) as response:
            if response.status_code == 200:
                posts = response.json()
                
                print("Первые 5 постов из JSONPlaceholder API:\n")
                print("=" * 50)
                
                for i in range(5):
                    post = posts[i]
                    print(f"Пост #{i+1}")
                    print(f"Заголовок: {post['title']}")
                    print(f"Текст: {post['body']}")
                    print("-" * 50)
            else:
                print(f"Ошибка: {response.status_code}")
                
    except requests.exceptions.RequestException as e:
        print(f"Ошибка: {e}")

# Запуск альтернативной версии
if __name__ == "__main__":
    get_posts_alternative()
Что делает этот код:

Отправляет GET-запрос к эндпоинту /posts публичного API JSONPlaceholder

Проверяет статус ответа (200 - успешно)

Извлекает данные из JSON-ответа

Выводит заголовки и тела первых 5 постов

Обрабатывает возможные ошибки соединения

Для работы скрипта необходимо установить библиотеку requests:

bash
pip install requests
Пример вывода:

text
Первые 5 постов из JSONPlaceholder API:

==================================================
Пост #1
Заголовок: sunt aut facere repellat provident occaecati excepturi optio reprehenderit
Текст: quia et suscipit...
--------------------------------------------------
Пост #2
Заголовок: qui est esse
Текст: est rerum tempore vitae...
--------------------------------------------------

Альтернативная версия с повторными запросами:
python
import requests

def get_weather_advanced():
    API_KEY = "ваш_api_ключ_здесь"
    BASE_URL = "http://api.openweathermap.org/data/2.5/weather"
    
    while True:
        print("\n" + "=" * 50)
        city = input("Введите название города (или 'выход' для завершения): ").strip()
        
        if city.lower() in ['выход', 'exit', 'quit']:
            print("Программа завершена.")
            break
            
        if not city:
            print("Ошибка: название города не может быть пустым!")
            continue
        
        params = {
            'q': city,
            'appid': API_KEY,
            'units': 'metric',
            'lang': 'ru'
        }
        
        try:
            response = requests.get(BASE_URL, params=params)
            data = response.json()
            
            if response.status_code == 200:
                # Извлекаем данные о погоде
                main_data = data['main']
                weather_info = data['weather'][0]
                
                print("\n" + "=" * 40)
                print(f"Погода в городе {data['name']}, {data['sys']['country']}:")
                print(f"🌡 Температура: {main_data['temp']}°C")
                print(f"📝 Описание: {weather_info['description'].capitalize()}")
                print(f"💨 Влажность: {main_data['humidity']}%")
                print(f"💨 Давление: {main_data['pressure']} hPa")
                print(f"🌬 Скорость ветра: {data['wind']['speed']} м/с")
                print("=" * 40)
                
            else:
                print(f"❌ Ошибка: {data.get('message', 'Город не найден')}")
                
        except requests.exceptions.RequestException as e:
            print(f"❌ Ошибка соединения: {e}")
        except Exception as e:
            print(f"❌ Произошла ошибка: {e}")

# Запуск расширенной версии
if __name__ == "__main__":
    get_weather_advanced()
Как получить API ключ:
Зарегистрируйтесь на OpenWeatherMap

Перейдите в раздел "API Keys" в личном кабинете

Скопируйте ваш API ключ

Замените "ваш_api_ключ_здесь" на ваш реальный ключ

Пример вывода программы:
text
Введите название города: Москва

========================================
Погода в городе Moscow, RU:
Температура: 15.5°C
Описание: Небольшая облачность
========================================
Что делает программа:
✅ Принимает название города от пользователя

✅ Отправляет GET-запрос к API OpenWeather с параметрами:

q - название города

appid - API ключ

units - единицы измерения (metric для °C)

lang - язык ответа

✅ Выводит текущую температуру и описание погоды

Обрабатывает ошибки (город не найден, проблемы с соединением и т.д.)

Для работы установите библиотеку requests:

bash
pip install requests

